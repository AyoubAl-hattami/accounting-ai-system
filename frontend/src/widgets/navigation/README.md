# Navigation Widget

Purpose: eventually compose permission-aware application navigation.

May later contain desktop/mobile navigation using app routes and shared UI. It
must not define business permissions, change route URLs, or own page content.

Status: scaffolding only. Existing AppShell navigation remains authoritative.

# Report Toolbar Widget

Purpose: eventually compose report filters and export actions.

May later contain date/account filters and the report-export feature. It must not
calculate report values, own report queries, or alter route URLs.

Status: scaffolding only. Existing report toolbars remain in current pages.

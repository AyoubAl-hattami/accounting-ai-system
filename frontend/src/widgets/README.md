# Widgets Layer

Purpose: compose entities and features into reusable application regions.

May later contain navigation, selectors, summaries, toolbars, and the assistant
drawer. It must not own route definitions, backend accounting policy, or
low-level transport.

Status: scaffolding only. Existing working components remain in current folders.

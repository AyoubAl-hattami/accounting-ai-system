# Page Migration Scaffolds

Purpose: stage route-level page composition without conflicting with current
feature pages.

May later contain approved page pilots composed from widgets, features, entities,
and shared UI. It must not be wired into routes until baseline parity is proven.

Status: scaffolding only. Existing pages and routes remain authoritative.

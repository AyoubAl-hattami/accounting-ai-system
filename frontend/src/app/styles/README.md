# App Styles

Purpose: connect global semantic theme styles to the application.

May later contain application-level style entry points and resets. It must not
contain feature-specific workflow logic or scattered one-off palette overrides.

Status: scaffolding only. Existing styles remain under `frontend/src/styles`.

# App Providers

Purpose: compose application-wide React providers.

May later contain authentication, company, locale, theme, query, and error
providers. It must not contain page UI, endpoint-specific logic, or accounting
calculations.

Status: scaffolding only and not wired into the app. Existing providers remain in
their current locations.

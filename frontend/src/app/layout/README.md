# App Layout

Purpose: compose application-wide shell and layout regions.

May later contain shell composition using navigation and company widgets. It
must not own account/journal/report workflows or duplicate shared UI primitives.

Status: scaffolding only. Existing layout components remain under
`frontend/src/components`.

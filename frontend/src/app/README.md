# Frontend App Layer

Purpose: compose the frontend application.

May later contain providers, routes, layout, and global style entry points. It
must not contain deep accounting logic, entity API implementations, or reusable
feature internals.

Status: scaffolding only. Existing working code remains in `frontend/src/features`,
`frontend/src/components`, and `frontend/src/routes`.

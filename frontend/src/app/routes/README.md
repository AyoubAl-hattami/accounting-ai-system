# App Routes

Purpose: compose route definitions and protection.

May later contain the existing URL map and route-level lazy loading. It must not
contain feature implementations, API mutations, or changed URLs hidden inside a
migration.

Status: scaffolding only and not wired. `frontend/src/routes` remains
authoritative.

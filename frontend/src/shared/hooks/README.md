# Shared Hooks

Purpose: hold generic React hooks with no business-feature dependency.

May later contain media, direction, focus, and reusable lifecycle hooks. It must
not fetch entity endpoints or import features, widgets, or pages.

Status: scaffolding only. Existing hooks remain in current feature folders.

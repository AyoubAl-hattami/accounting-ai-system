# Shared Internationalization

Purpose: centralize locale and text-direction infrastructure.

May later contain translation access, locale types, direction helpers, and
formatting integration. It must not contain page components or silently rename
existing translation keys.

Status: scaffolding only. `frontend/src/i18n` remains authoritative.

# Shared Theme

Purpose: centralize semantic visual tokens.

May later contain CSS variables, Tailwind token mappings, typography, elevation,
financial colors, and chart palettes. It must not contain page logic or
provider-specific assistant branding.

Status: scaffolding only. Existing theme behavior remains in current styles and
configuration.

# Shared Layer

Purpose: provide reusable, business-neutral frontend foundations.

May later contain UI primitives, theme, transport, utilities, generic hooks,
i18n infrastructure, and cross-cutting types. It must not import entities,
features, widgets, or pages.

Status: scaffolding only. Existing working shared code remains in current
frontend folders.

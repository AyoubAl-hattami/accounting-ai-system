# Shared Types

Purpose: hold truly cross-cutting technical TypeScript types.

May later contain generic pagination, result, or utility types. It must not
become a dumping ground for all API DTOs or create feature-to-feature coupling.

Status: scaffolding only. Existing types remain in current API and feature files.

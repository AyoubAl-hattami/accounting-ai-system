# Shared Library

Purpose: hold small framework-neutral reusable utilities.

May later contain date, money, formatting, and safe helper functions. It must not
contain feature orchestration, route state, API clients, or accounting policy
that belongs on the backend.

Status: scaffolding only. Existing utilities remain in their current locations.

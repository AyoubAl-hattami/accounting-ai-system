# Shared UI

Purpose: provide accessible, business-neutral UI primitives.

May later contain buttons, fields, modals, drawers, tables, page headers, money
display, badges, and feedback states. It must not fetch APIs, inspect company
roles, or own accounting workflows.

Status: scaffolding only. Existing components remain under
`frontend/src/components`.

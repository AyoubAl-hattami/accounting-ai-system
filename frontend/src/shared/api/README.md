# Shared API

Purpose: provide HTTP transport mechanics.

May later contain the Axios client, interceptors, cancellation, and generic error
normalization. It must not become a flat store of all feature endpoints or own
permissions/business rules.

Status: scaffolding only. `frontend/src/api` remains authoritative.

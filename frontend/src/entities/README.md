# Entities Layer

Purpose: expose reusable frontend representations of business concepts.

May later contain typed APIs, query keys, models, mappers, formatters, and small
entity UI. It must not own route composition or multi-step user workflows.

Status: scaffolding only. Existing working entity logic remains in
`frontend/src/features` and `frontend/src/api`.

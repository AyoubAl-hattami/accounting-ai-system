# User Entity

Purpose: expose reusable user identity and status presentation.

May later contain user types, API mappings, and small display components. It
must not own authentication providers, global administration workflows, or
backend authorization rules.

Status: scaffolding only. Existing user code remains in current folders.

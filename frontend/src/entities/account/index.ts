/**
 * Account entity types.
 *
 * These are the canonical frontend representations of the accounting
 * chart-of-accounts domain.  They mirror the backend AccountRead schema.
 */
export type { Account } from '../../api/types';
export type { AccountSeedResult } from '../../api/types';

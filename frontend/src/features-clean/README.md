# Feature Migration Scaffolds

Purpose: stage isolated user-action modules without conflicting with the current
`frontend/src/features` tree.

May later contain approved feature pilots using entities and shared modules. It
must not be imported by the running app until a specific migration is approved
and baseline-verified.

Status: scaffolding only. Existing working features remain authoritative.

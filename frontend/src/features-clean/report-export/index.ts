/**
 * Reports feature (clean architecture pilot).
 *
 * Not wired into app routes until baseline parity is verified.
 */
export {
  fetchTrialBalance,
  fetchProfitAndLoss,
  fetchBalanceSheet,
  fetchAccountLedger,
  fetchGeneralLedger,
  downloadReport,
} from './api';

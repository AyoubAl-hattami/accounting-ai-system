# Reports Application

Purpose: coordinate authorized accounting report queries.

May later contain report query DTOs, reader ports, and report result contracts.
It must not contain HTTP/CSV/PDF presentation, provider SDKs, or unrestricted
SQLAlchemy queries.

Status: scaffolding only. Current report behavior remains in
`backend/app/modules/accounting`.

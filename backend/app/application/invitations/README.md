# Invitations Application

Purpose: coordinate invitation create, validate, accept, cancel, and reissue use
cases.

May later contain invitation commands, lifecycle orchestration, DTOs, and ports.
It must not contain raw-token logging, HTTP responses, concrete row locking, or
independent commits.

Status: scaffolding only. Current invitation behavior remains under
`backend/app/modules/accounting`.

# Companies Application

Purpose: coordinate company and membership use cases.

May later contain company commands, queries, authorization orchestration, DTOs,
and ports. It must not contain route dependencies, ORM query implementations, or
frontend permission logic.

Status: scaffolding only. Current company behavior remains in
`backend/app/modules/accounting`.

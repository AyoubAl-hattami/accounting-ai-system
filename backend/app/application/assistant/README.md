# Assistant Application

Purpose: coordinate provider-neutral accounting assistant use cases.

May later contain intent, grounding, answer, journal-proposal, conversation, and
confirmation use cases and ports. It must not contain Gemini/OpenAI SDK calls,
provider prompts, FastAPI routes, or direct mutation commits.

Status: scaffolding only. Current assistant behavior remains in
`backend/app/modules/accounting`.

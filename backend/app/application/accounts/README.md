# Accounts Application

Purpose: coordinate account commands and queries.

May later contain account use cases, DTOs, and repository ports. It must not
contain ORM query implementations, FastAPI routes, UI concerns, or unrelated
fiscal/journal workflows.

Status: scaffolding only. Current account behavior remains in
`backend/app/modules/accounting`.

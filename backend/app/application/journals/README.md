# Journals Application

Purpose: coordinate journal lifecycle use cases.

May later contain create, update, review, post, reverse, void, and
opening-balance commands and queries. It must not contain HTTP translation,
SQLAlchemy implementations, provider SDK calls, or duplicated domain rules.

Status: scaffolding only. Current journal code remains under
`backend/app/modules/accounting` until high-risk verification is complete.

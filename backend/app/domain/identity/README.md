# Identity Domain

Purpose: define stable identity values and policies.

May later contain normalized email value objects and identity invariants. It must
not contain JWT transport, password-hashing adapters, ORM user queries, or API
schemas.

Status: scaffolding only. Existing identity code remains authoritative until
migrated safely from the current backend modules.

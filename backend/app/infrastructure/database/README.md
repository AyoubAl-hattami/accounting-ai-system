# Database Infrastructure

Purpose: group persistence implementations and database composition.

May later contain unit-of-work implementations and database-specific adapters.
It must not contain domain policy, FastAPI routes, frontend contracts, or
migration-history rewrites.

Status: scaffolding only. Existing database code and accounting models remain in
their current locations.

# Export Infrastructure

Purpose: implement outbound accounting report formats.

May later contain CSV and PDF presenters/exporters. It must not calculate
authoritative report totals, query unrestricted company data, or define API
authorization.

Status: scaffolding only. Current export services remain under
`backend/app/modules/accounting`.

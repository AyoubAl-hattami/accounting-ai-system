# Security Infrastructure

Purpose: implement security-related application ports.

May later contain JWT, password hashing, token, and security storage adapters. It
must not contain route authorization policy, company role decisions, domain
identity policy, or exposed secrets.

Status: scaffolding only. Current security code remains under `backend/app/core`
and existing accounting modules.
